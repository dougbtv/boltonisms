[Unit]
Description=Boltonisms Data Rebuild Timer
Requires=boltonisms-build.service

[Timer]
# Run every hour
OnCalendar=hourly
# Run 5 minutes after boot
OnBootSec=5min
# Ensure it runs even if it missed the scheduled time
Persistent=true

[Install]
WantedBy=timers.target
